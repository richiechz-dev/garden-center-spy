[project]
name = "vivero-spy"
version = "0.1.0"
description = "Scraper para extraer precios de articulos de plantas desde endpoints JSON"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]
